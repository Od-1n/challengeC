#include <stdio.h>

int main (){

float tempc , tempf;
     printf("entrer la temperature en f");
        scanf("%f",&tempf);
            printf("la temperature en c est %.f",(tempf-32)/1.8);
    return 0;
}